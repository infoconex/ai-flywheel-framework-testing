schema_version: 1
id: prompt-017-self-host-certification
framework_revision: 18335e57165a8984adab4790d3a6210355b484ba
scenario:
  id: 10
  name: self-hosting
  objective: >-
    Use the AI Flywheel's own mission, goal, execution, evidence, validation,
    classification, adaptation, persistence, reuse, certification, readiness,
    and approval-boundary contracts to evaluate its certification package.
  expected_result: passed
  certification_expected_result: failed
  readiness_expected_result: failed
  expected_blockers:
    - Prompt 001 retained evidence lacks an exact tested framework commit SHA.
    - Prompt 002 retained evidence lacks an exact tested framework commit SHA.
  required_behavior:
    - Preserve unknown tested framework revisions as null.
    - Record the immutable testing evidence revision separately.
    - Create findings and corrective rerun actions for Prompt 001 and Prompt 002.
    - Allow the self-hosting execution to succeed with goal-blocked disposition.
    - Keep the certification goal blocked.
    - Do not invent human approval.
    - Do not propose or perform a ready-for-missions state transition.
    - Do not modify the framework repository or durable state.
cleanup: not-applicable-in-memory-fixture
